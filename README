Installation:
   	git clone https://github.com/juliendz/dotvim.git ~/.vim


Create symlinks:
	ls -s ~/.vim/vimrc ~/.vimrc
	ls -s ~/.vim/gvimrc ~/.gvimrc

Updating local .vim to latest changes:
	git pull
	git submodule init
	git submodule update
	
Windows:
	Copy gvimrc to $VIM/_vimrc
	

